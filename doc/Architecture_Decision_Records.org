#+options: todo:t
#+title: Architecture Decision Records
#+date: <2024-03-08 Fr>
#+author: Christian Egli
#+email: christian.egli@sbs.ch
#+language: en

#+TODO: DRAFT PROPOSED | ACCEPTED REJECTED DEPRECATED SUPERSEDED

# New entries go at the top of this file; existing entries are not
# reordered when they're revised.

* ACCEPTED Opcodes deliberately not implemented: ~uplow~, ~locale~, ~backmatch~, ~compdots~, ~nobreak~, ~macro~
- Deciders :: CE
- Date :: [2026-08-25 Di]

** Context and Problem Statement

A gap audit (diffing liblouis's ~opcodeNames~ table against every
opcode string louis-rs's parser recognises) turned up 6 opcodes with
no parser support:

- ~uplow~ :: is deprecated upstream.
- ~backmatch~ :: not documented anywhere in liblouis's own manual.
- ~compdots~ :: liblouis's manual says ~FIXME: the compdots opcode
  should be documented~.
- ~nobreak~ :: an undocumented character-class opcode. No usage
  example anywhere to infer intended semantics.
- ~macro~ :: a macro-definition mechanism for expanding a
  table-defined name into other opcodes.

None of the six appear in any table under ~tables/~, nor in any test
under ~tests/~. There is no known liblouis table, current or
historical, that exercises any of them.

** Decision Drivers

- Two of the six are deprecated by liblouis itself -- implementing
  them would mean maintaining compatibility with something upstream
  has already abandoned.
- Three of the six are undocumented even in liblouis's own manual --
  implementing them correctly would mean reverse-engineering exact
  semantics from C source with no table or test to validate against,
  a poor cost/confidence tradeoff for opcodes nothing uses.
- Zero real-world tables (liblouis's own corpus, the closest available
  proxy for "real-world") exercise any of the six.

** Decision Outcome

Do not implement ~uplow~, ~locale~, ~backmatch~, ~compdots~,
~nobreak~, or ~macro~.

** Links

- liblouis's opcode name table: ~compileTranslationTable.c~,
  ~opcodeNames[CTO_None]~
- liblouis's opcode reference: ~doc/liblouis.texi~, "Braille Indicator
  Opcodes" and surrounding sections

* ACCEPTED Give ~CompiledRegexp::find~ an inline bitset and fixed-capacity thread buffer, swapped by reference
- Deciders :: CE
- Date :: [2026-08-20 Do]

** Context and Problem Statement

Profiling a full liblouis-test-suite run showed around 41% of total
self-time going to the memory allocator, traced to
~CompiledRegexp::find~: every call allocates two fresh ~ThreadList~s
(the Pike-VM "current"/"next" thread sets), each carrying a ~seen:
Vec<bool>~ sized to the compiled program's instruction count, to dedup
epsilon-closure visits within ~add_thread~. Since ~find~/~is_match~
back every ~match~/~context~ rule check and run at every input
position across millions of translation calls, this
allocate-and-discard cycle dominated the profile.

A follow-up question reframed the problem: liblouis' ~match~/
~context~/~correct~/passN patterns aren't arbitrary regexps, they're
drawn from a known, surveyable corpus (the liblouis tables). Compiling
every such pattern from six tables chosen for having the most
~match~/~context~ rules (~en-ueb-g2.ctb~, three Vietnamese tables,
~fil-g2.ctb~, ~en-g3.ctb~) and measuring ~CompiledRegexp~'s
instruction count and epsilon/~Split~ density gave 5,457 real data
points:

| table            |    n | len (min/mean/median/p99/max) | splits (mean/max) |
|------------------+------+-------------------------------+-------------------|
| en-ueb-g2.ctb    | 2192 | 4 / 22.5 / 27 / 38 / *42*     | 3.4 / 9           |
| vi-vn-g2.ctb     | 1170 | 4 / 10.4 / 10 / 15 / *16*     | 1.1 / 3           |
| vi-saigon-g1.ctb |  912 | 4 / 11.2 / 11 / 16 / *16*     | 1.4 / 3           |
| vi-vn-g1.ctb     |  814 | 4 / 10.3 / 10 / 16 / *16*     | 1.1 / 3           |
| fil-g2.ctb       |  242 | 4 / 31.0 / 37 / 43 / *44*     | 6.1 / 8           |
| en-g3.ctb        |  127 | 4 / 13.1 / 17 / 17 / *17*     | 2.5 / 4           |

Not one of above programs exceeded 44 instructions, but a follow-up
sweep of every loadable table in liblouis (397 files, 363 loaded as
standalone tables, 31,023 compiled patterns across 233 tables)
confirmed the general shape (mean 8.9, median 5, p99 37) while finding
a real outlier: ~afr-za-g2.ctb~ (Afrikaans grade 2) has a pattern of
*508* instructions, and several other tables (~hu-hu-g2.ctb~,
~mn-MN-g2.ctb~, ~zu-za-g2.ctb~, ~xh-za-g2.ctb~, among others) exceed
100. These are legitimate patterns, not degenerate input: large
~match~ alternations of literal word-ending strings, e.g.
~afr-za-g2.ctb~'s ~AAK|aak|AAL|aal|AFEL|afel|...~ (50+ alternatives),
where each alternative costs roughly ~chars + Split + Jump~
instructions. Across the full 31,023-pattern corpus, 65 (0.21%) exceed
44 instructions, 34 (0.11%) exceed 64, and 18 (0.06%) exceed 128.

~seen~ isn't sparse in the usual sense -- for the overwhelming
majority of real patterns the whole index space is just small. That
ruled out a hash-based or sparse-set structure (still allocates on
first insert, and pays hashing overhead for no benefit over plain
indexing at this size) and pointed at a fixed-size bitmask instead,
with a heap fallback now sized for a real, measured minority of
patterns that don't fit -- not just a theoretical one.

** Decision Drivers

- ~find~/~is_match~ are called an enormous number of times during a
  single translation run (millions, per the profile) -- any per-call
  fixed cost dominates.
- The same compiled ~CompiledRegexp~ can be matched concurrently
  against different input from multiple ~rayon~ worker threads, so any
  reused scratch state must not require synchronization/locking, and
  must stay correct under concurrent, independent use.
- Capture positions (~CaptureStart~/~CaptureEnd~) and runtime-
  conditional epsilon edges (~VariableEqual~/~NotVariableEqual~ on
  ~Environment~, ~AssertEnd~/~AssertMoreInput~ on input position) are
  inherently per-call, per-position data -- whatever the fix, it can't
  assume the epsilon-closure computation itself can be hoisted to
  compile time.
- Minimize code complexity/invasiveness given this is a hot-path fix,
  not a rewrite: ~find~'s own control flow (the ~while~ loop,
  ~add_thread~ recursion, ~mem::swap~) should stay unchanged.
- Whatever bound on program size/thread count a fix relies on, it must
  be a safety margin, not an assumption baked in as a hard limit --
  some future table's pattern exceeding what's been surveyed so far
  should degrade gracefully (fall back to heap storage), not panic or
  silently misbehave.

** Considered Options

- Status quo :: ~Vec<bool>~ ~seen~, fresh ~ThreadList::new()~
  allocated on every ~find~ call.
- Generational ~seen~ + per-thread pool :: revert to ~seen: Vec<u32>~
  stamped with a per-~ThreadList~ generation counter (bumped, not
  cleared, each step), and pool ~(current, next)~ pairs in a
  ~thread_local!~ ~RefCell<Vec<...>>~, checked out at the start of
  ~find~ and returned at the end.
- Precompute epsilon closures at compile time :: eliminate
  ~add_thread~'s recursive walk by precomputing, per instruction, the
  reachable consuming/~Match~ instructions.
- Convert the NFA to a DFA :: subset-construct the compiled program
  into a DFA with O(1) per-byte transitions and no thread-list/
  epsilon-closure bookkeeping at all.
- Inline bitset + fixed-capacity thread buffer :: since every
  surveyed program fits in 64 instructions, give ~ThreadList~ a ~seen~
  bitset (a ~SmallVec<[u64; 1]>~-backed bitmask, 64 bits inline) and a
  ~threads: SmallVec<[Thread; 64]>~, both stack-resident with no heap
  allocation for any real pattern, falling back to heap storage only
  if a pattern ever exceeds that.

** Decision Outcome

Chosen option: *Inline bitset + fixed-capacity thread buffer* (option
5).

Option 2 (generational ~seen~ + per-thread pool) was fully
implemented, tested, and benchmarked in an earlier pass: ~find~'s own
body stayed untouched, ~ThreadList~ gained a ~generation: u32~ field,
and a ~thread_local!~ ~RefCell<Vec<...>>~ pool replaced the two
~ThreadList::new()~ calls -- mirroring how RE2's ~NFA~ class and
Rust's ~regex-automata~ (~Cache~/~create_cache()~) solve the identical
tension between reusing scratch state and staying correct under
concurrent use of one compiled program. It measured a genuine 9--14%
end-to-end ~Translator::translate~ speedup, but was reverted in favor
of the simpler status quo: the ~thread_local!~ pool, generation
counter, and wraparound-reset branch were judged more state than the
win justified at the time.

Surveying real liblouis patterns (see Context) reopened the question
with a fact the earlier options didn't have: program size is not just
"probably small," it's measured -- 99.89% of 31,023 real compiled
patterns fit in 64 instructions, with a known, quantified minority
(0.11%) that don't. That makes option 5's fixed-capacity inline
storage a sized bet, not a guess, with a heap fallback for the
measured minority rather than a hypothetical one -- and unlike option
2, it needs no pool, no thread-local state, and no generation counter,
since a plain "clear the few inline words to zero" is already O(1) at
this size.

Implementing it first hit a real regression: making ~ThreadList~ ~1.5KB
(a ~SmallVec<[Thread; 64]>~ is largely inline capacity, 24 bytes per
~Thread~ x 64) while still swapping it *by value* every input character
(~std::mem::swap(&mut current, &mut next)~, once per step, not once per
~find~ call) made ~translate~ 37--51% *slower* -- the per-step swap
cost of ~1.5KB dwarfed the allocation savings. The fix: keep the two
~ThreadList~s in fixed stack slots and make ~current~/~next~
*references* into them, so the per-step swap exchanges two pointers,
not ~1.5KB of storage.

With that fix, the benchmark showed 42--45% faster end-to-end
~translate~ -- three to four times option 2's win, with less code
complexity, not more. Verified correct against all unit tests and the
full 2,221,196-test liblouis suite (identical pass/fail/xfail
percentages).

Options 3 and 4 were both discussed and set aside, not rejected
outright:

- Option 3 (precomputed closures) only partially applies: ~Jump~/
  ~Split~ chains are static and could be precomputed, but
  ~VariableEqual~/~NotVariableEqual~ (gated on ~Environment~) and
  ~AssertEnd~/~AssertMoreInput~ (gated on input position) are runtime-
  conditional, so ~add_thread~ would still need to walk those edges
  live every step -- and it's orthogonal to this fix anyway:
  precomputing reachability doesn't remove the need for *somewhere to
  hold the current step's active thread set*, so ~ThreadList~'s
  ~threads~/~seen~ storage would still be needed regardless.
- Option 4 (DFA) can't preserve per-thread capture positions --
  collapsing NFA states into DFA states loses exactly the
  ~(usize, usize)~ capture span every ~match~/~context~ rule relies on
  to build its ~ResolvedTranslation~. Production engines that use a
  DFA (RE2, Rust's ~regex-automata~) do so only for a fast existence/
  boundary check, falling back to NFA simulation specifically to
  extract captures -- adopting that would mean building and
  maintaining two execution models.

*** Positive Consequences

- 42--45% faster end-to-end ~Translator::translate~ (measured via
  ~benches/translate.rs~ against ~en-ueb-g2.ctb~, word/sentence/
  paragraph inputs, p < 0.05) -- three to four times option 2's
  measured win.
- No heap allocation at all in ~find~ for 99.89% of real patterns (the
  full 397-table, 31,023-pattern sweep), not just fewer/amortized
  allocations as option 2 gave.
- No new persistent state: no ~thread_local!~, no pool checkout/return
  bookkeeping, no generation counter or wraparound branch -- ~seen~'s
  ~clear()~ is a plain zero-fill of one or two ~u64~s, correct
  unconditionally.
- Correctness unaffected: all unit tests pass, clippy is clean on the
  new code, and the full liblouis suite (2,221,196 tests via ~check~)
  produces identical pass/fail/xfail percentages before and after.
- Degrades gracefully rather than breaking for the real patterns known
  to exceed the inline capacity (34 of 31,023 in the full sweep, up to
  508 instructions in ~afr-za-g2.ctb~): ~SmallVec~ spills to the heap
  exactly as ~Vec~ always did, so this is a known, quantified minority
  case handled by design, not an unproven edge case.

*** Negative Consequences

- Adds a new dependency (~smallvec~) and a small hand-rolled ~Bitset~
  type, versus option 1's plain ~Vec<bool>~ or the standard-library-
  only status quo.
- The chosen capacity (64) is deliberately not a hard bound -- a full
  397-table sweep found 34 real patterns (0.11%) that exceed it, up to
  508 instructions in ~afr-za-g2.ctb~'s large word-ending alternations.
  Correctness doesn't depend on it (the heap fallback covers that), but
  those patterns specifically don't get the inline-storage benefit,
  with no visible signal when that happens.
- ~find~'s call sites for ~add_thread~ changed from passing
  ~&mut current~/~&mut next~ (reborrowing an owned value) to passing
  ~current~/~next~ directly (already ~&mut ThreadList~) -- a small but
  real increase in how much a reader needs to track reference vs.
  value semantics in this function, compared to option 1.

** Pros and Cons of the Options

*** Status quo

- Good, because it's the simplest possible code -- no pool, no
  generation counter, no wraparound edge case, no new dependency
- Bad, because it's what the profile identified as ~41% of total
  self-time in a full test-suite run, leaving a measured 42--45%
  ~translate~ speedup (option 5) unclaimed

*** Generational ~seen~ + per-thread pool

- Good, because it directly targets the allocation cost the profile
  measured, not just the clearing cost
- Good, because it requires no changes to ~find~'s matching logic, and
  mirrors how RE2/~regex-automata~ solve the same problem
- Good, because it was actually built and measured: a genuine 9--14%
  ~translate~ speedup, with correctness verified against the full
  liblouis suite
- Bad, because it reintroduces the ~u32~ wraparound question 3d0ebac6
  explicitly wanted to avoid (mitigated: reset-on-wraparound branch,
  practically unreachable)
- Bad, because a per-thread pool that only ever grows (never shrinks)
  means a program that used one large pattern once and then only small
  ones keeps that oversized ~ThreadList~ allocation alive for the
  process's lifetime

*** Precompute epsilon closures

- Good, because it would reduce ~add_thread~'s own recursive-call
  overhead, a real (if smaller) line item in the profile
- Bad, because runtime-conditional edges (~VariableEqual~/~AssertEnd~/
  etc.) can only be partially precomputed -- the live walk, and
  therefore the per-step thread list and its dedup, can't be
  eliminated
- Bad, because it's a materially larger, more invasive change for a
  benefit that hasn't been profiled/quantified yet

*** Convert NFA to DFA

- Good, because DFA execution has no per-step thread-list/epsilon-
  closure bookkeeping at all -- O(1) per byte
- Bad, because it can't represent per-thread capture positions without
  a second, NFA-based execution path for captures (as RE2/
  ~regex-automata~ do) -- effectively building and maintaining two
  engines
- Bad, because our patterns are short and locally-anchored, not the
  long free-text scans DFA engines are built to accelerate, so the win
  is unclear relative to the cost

*** Inline bitset + fixed-capacity thread buffer

- Good, because it eliminates the allocation itself for 99.89% of real
  patterns (397-table, 31,023-pattern sweep), not just the clearing
  cost or the frequency of allocation
- Good, because the capacity choice is grounded in a real, diverse,
  full-corpus survey, not a guess from a handful of tables -- and the
  survey itself found and quantified the minority (0.11%, up to 508
  instructions in ~afr-za-g2.ctb~) that doesn't fit
- Good, because it needs no persistent/pooled state at all -- no
  ~thread_local!~, no generation counter, no wraparound branch
- Good, because it degrades gracefully (heap fallback via ~SmallVec~)
  for that measured minority rather than being unsound
- Good, because it measured 3--4x option 2's win (42--45% vs. 9--14%)
- Bad, because it adds a new dependency (~smallvec~)

** Links

- Related to :: [[*Hand-rolled regexp VM instead of the ~regex~/~regex-automata~ crates][Hand-rolled regexp VM instead
  of the ~regex~/~regex-automata~ crates]] -- this decision operates
  entirely within the VM that ADR chose to keep
- Related to :: 3d0ebac6b1dd1054a6d840920be22f7342dfb6a1 ("Simplify
  ThreadList's visited-set to a plain cleared Vec<bool>") -- ~seen~
  ends up neither that commit's ~Vec<bool>~ nor the generation counter
  it replaced, but a fixed-size bitset; the O(1)-clear property that
  commit gave up is regained as a side effect of the bitset being
  small, not by reintroducing generations

* DRAFT Isolated single-letter capitalization: capsletter vs. begcapsword tier selection
- Deciders :: CE
- Date :: [2026-08-18 Di]

** Context and Problem Statement

~de-g0-detailed.utb~ fails 62/77 forward tests in
~de-g0-detailed-specs.yaml~ by leaking a stray Private Use Area
codepoint: ~"A"~ produces ~⠨\x{f1200}⠁~ instead of ~⠘⠁~. Trace:

#+begin_example
1 │      │ ⠨  │ capsletter ⠨
2 │      │ 󱈀  │ context `[]$U~ 󱈀
3 │ A    │ ⠁  │ base uppercase A a
#+end_example

The table declares ~capsletter 46~ (⠨), but the German Systematik
wants dots 45 (⠘, same as ~begcapsword 45~) for an isolated capital.
liblouis's ~capsletter~ opcode takes one dot pattern with no way to
special-case "unless this letter is also a whole word/segment", so
the table works around it (~de-g0-core.uti:665-675~, comment inline):

#+begin_example
# According to the Systematik single letter capitals are to be
# announced with 45. This is not supported by liblouis so we use a
# context rule to mark those places and fix them later in a pass2 rule below
noback context `[]$U~ @ad
noback context !$U[]$U!$l @ad
noback pass2 @46-ad-46 @45
#+end_example

A zero-width ~context~ rule stamps a virtual-dot marker (~@ad~) after
an isolated capital not followed by a letter; ~pass2~ collapses the
literal sequence /46, ad, 46/ into /45/. This only works because
liblouis's real per-position loop, after inserting the zero-width
marker, revisits the same position and its independent generic
capsletter check (~CAPSEMPH~, ~lou_translateString.c~) fires again —
coincidentally producing the second ~46~ the ~pass2~ rule expects. Two
unrelated mechanisms colliding at one position by accident, not a
documented feature. The same hack shape recurs for numeric indication
in the same table (~"'56"~/~".303"~ leak two other PUA codepoints the
same way).

Our indication architecture (see [[*ACCEPTED Pre-compute indication events before translation][ADR: Pre-compute indication events
before translation]]) computes capsletter/begcapsword placement in one
immutable pass /before/ translation, independent of the ~context~
engine. Confirmed our ~pass2~ engine correctly collapses a genuine
~46-ad-46~ sequence when one is actually present, so the gap is
upstream: our precompute never emits the second ~46~, so the marker
leaks through unconverted.

*** A tempting but empirically false generalization

~"kW kVA hPa"~ expects ~⠅⠘⠺ ⠅⠘⠧⠁ ⠓⠨⠏⠁~: the lone capital in ~"kW"~
(word boundary follows) gets /45/, the lone capital in ~"hPa"~ (more
letters follow) gets /46/ — exactly the ~needs_close~ signal
~upper_runs()~ already computes. Looked like the fix: ~emit_whole_word()~
and ~emit_run()~ (~src/translator/indication/uppercase.rs~) both check
~letter_count == 1~ and jump to ~capsletter~ before ever consulting
~begcapsword~/~needs_close~; reordering so a run that doesn't need
closing prefers ~begcapsword~ regardless of length looked like a
strict generalization.

Prototyped and run against the full corpus (2,221,658 tests) — *wrong*
as a general rule. Regressed dozens of unrelated tables, with zero
gain on the target file (the leak just changed shape, since ~pass2~'s
precondition no longer matches once the main stage emits ~45~ directly):

| Table                          | Before | After  | Change  |
|---------------------------------+--------+--------+---------|
| ~tr.yaml~                       |  60.4% |  45.3% | −15.1pp |
| ~en-ueb-symbols_harness.yaml~   |  99.5% |  68.5% | −31.0pp |
| ~eo-g1_harness.yaml~            |  99.6% |  78.6% | −21.0pp |
| ~ga-g1_harness.yaml~            | 100.0% |  88.2% | −11.8pp |
| ~el-forward.yaml~               |  87.1% |  62.4% | −24.7pp |
| ~pl-g1.yaml~                    |  90.1% |  72.3% | −17.8pp |
| ~de-g0-detailed-specs.yaml~     |  63.4% |  63.4% |    flat |

Every regressed table defines ~begcapsword~ as the /same/ cell as
~capsletter~, just doubled (e.g. ~tr-g2.ctb~: ~capsletter 6~,
~begcapsword 6-6~) — capsletter-for-isolated-singles is the right
default almost everywhere. The German table is a genuine outlier:
its 46-vs-45 values are unrelated, and it specifically wants the
word-tier form for a standalone letter. A few other shipped tables
share that "unrelated, not doubled" shape (~bg.utb~, ~da-dk-g16~/
~g26~, ~lt-6dot.utb~, ~mk-g1.utb~, ~nl-NL-g0.utb~, ~no-no-g0.utb~,
~ru-comp6.utb~, ~sl-si-g1.utb~, ~vi-vn-g0/g1/g2~) and may or may not
have the same real need — untested, none of their suites exercise an
isolated single-letter word.

** Decision Drivers

- Replicating liblouis's re-entrant quirk is explicitly ruled out
- Any fix must not regress the tables that correctly rely on
  capsletter-first behavior for isolated single letters
- Prefer addressing the actual capability gap (liblouis's opcode
  grammar can't express a per-table exception here) over a
  table-specific special case hardcoded into the engine
- A clean mechanism would make the German ~context~/~pass2~ hack
  unnecessary, worth proposing for removal upstream (the author's
  employer is a liblouis co-maintainer) — but that's a follow-on
  action gated on a real fix landing, not a substitute for one

** Considered Options

1. *Replicate the liblouis quirk* — make ~context~ zero-width
   insertions retrigger the per-position indicator check. Ruled out:
   this is the problem the table works around, not a solution, and
   reintroduces the re-entrant state our precomputed architecture
   avoids.

2. *Blanket engine change* — prefer ~begcapsword~ over ~capsletter~
   whenever a run doesn't need closing, regardless of length.
   Prototyped, empirically falsified (see table above). Ruled out.

3. *Status quo* — accept as a known limitation. Zero risk and cost,
   doesn't solve the goal.

4. *New louis-rs table extension* — a declarative opt-in signal (e.g.
   ~#-single-letter-caps-tier: begcapsword~) letting a table request
   the word tier for non-closing single-letter runs explicitly. Zero
   blast radius on tables that don't opt in; lets the German hack be
   deleted upstream once implemented. Not liblouis-compatible unless
   liblouis adopts the same directive.

5. *Structural fix: interleave context-pattern application with
   indicator computation* — liblouis's loop applies ~context~ rules
   and the indicator check in the same per-position pass, which is why
   they can interact at all. Our architecture deliberately keeps them
   separate (see the pre-compute ADR). This option would let a
   zero-width ~context~ insertion influence indicator selection at
   that position — modeling the real interaction the table's rule
   wants, not liblouis's specific double-firing. Not yet scoped: needs
   a concrete signal/direction between the two subsystems, and a plan
   for tables using ~context~/~pass2~ for unrelated purposes.

6. *Upstream table cleanup* — remove the now-superfluous ~context~/
   ~pass2~ hack (caps and numeric) from ~de-g0-core.uti~ once option 4
   or 5 lands. Not standalone; depends on one of them landing first.

** Decision Outcome

Not yet decided. Option 2 — the "obvious general fix" — is now known
wrong. The live choice is between option 3 (accept the gap), option 4
(a new declarative extension: scoped and low-risk, diverges from
strict liblouis compatibility), and option 5 (a deeper architectural
change: unscoped, would make the feature compose the way the table
author intended, but touches a boundary deliberately decoupled for
reasons already documented in this file).

** Links

- [[*ACCEPTED Pre-compute indication events before translation][ADR: Pre-compute indication events before translation]] — the
  architecture this problem sits on top of
- ~src/translator/indication/uppercase.rs~ — ~emit_whole_word~,
  ~emit_run~, ~upper_runs~ (the ~needs_close~ signal)
- ~src/translator/table/multipass.rs~ — verified working correctly in
  isolation against a synthetic ~46-ad-46 -> 45~ pass2 rule
- ~/home/eglic/src/liblouis/tables/de-g0-core.uti:665-676~ — the
  ~context~/~pass2~ workaround and its own comment acknowledging the
  liblouis limitation
- [[https://github.com/liblouis/liblouis/blob/master/liblouis/lou_translateString.c][liblouis lou_translateString.c]] — ~CAPSEMPH~/~capsletterDefined~,
  the per-position re-entrancy this ADR is about

* ACCEPTED Unify word/number-boundary checks onto class-constraint lookaround
- Deciders :: CE
- Date :: [2026-08-18 Di]
- Implemented :: [2026-08-19 Mi]

** Context and Problem Statement

~midword ! ⠠⠖~ fires on an isolated ~"!"~ with nothing around it,
producing ~⠠⠖~ instead of ~⠖~. Cause: ~Rule::Midword~ compiles to
~Boundary::NotWord~ transitions, evaluated by
~is_word_start~/~is_word_end~. ~is_word_start~ gates on ~is_word(c)~
before ever consulting the true preceding character, so for a rule
whose own text isn't a letter, the gate fails and the check
degenerates to "always true" regardless of what actually precedes the
match — the true neighbor is never consulted. Correct by accident for
ordinary letter-based rules (gate always passes), wrong for
punctuation.

liblouis itself computes ~beforeAttributes~/~afterAttributes~ from
~input->chars[pos-1]~ and ~input->chars[pos+length]~. Every
word/number opcode reduces to one shape, "does the real neighbor's
attributes intersect a required set", differing only in which set
(Letter, Space, Punctuation, Digit, SeqDelim):

| Opcode       | before                            | after                             | combine    |
|--------------+-----------------------------------+-----------------------------------+------------|
| ~word~       | Space,Punctuation                 | Space,Punctuation                 | AND        |
| ~begword~    | Space,Punctuation                 | Letter                            | AND        |
| ~begmidword~ | Letter,Space,Punctuation          | Letter                            | AND        |
| ~midword~    | Letter                            | Letter                            | AND        |
| ~midendword~ | Letter                            | Letter,Space,Punctuation          | AND        |
| ~endword~    | Letter                            | Space,Punctuation                 | AND        |
| ~partword~   | Letter                            | Letter                            | *OR*       |
| ~sufword~    | Space,Punctuation,SeqDelim        | Space,Letter,Punctuation,SeqDelim | AND        |
| ~prfword~    | Space,Letter,Punctuation,SeqDelim | Space,Punctuation,SeqDelim        | AND        |
| ~begnum~     | Space,Punctuation                 | Digit                             | AND        |
| ~midnum~     | Digit                             | Digit                             | AND        |
| ~endnum~     | Digit                             | —                                 | —          |
| ~decpoint~   | Digit                             | Digit (required)                  | reclassify |

~word~ uses ~Boundary::Word~ in our code, same gate bug and
same-too-broad-neighbor-set issue as noted below. ~decpoint~ doesn't
go through ~Boundary~ at all in either engine's sense we've been
discussing — liblouis reclassifies it to ~midnum~ on the spot if a
digit precedes; our own ~Decpoint~ arm skips the trie boundary
mechanism entirely and instead tags the character for the numeric
indicator/constrainer as if it were a ~midnum~ character. No live bug
there, just a different existing mechanism — not part of this ADR's
fix.

liblouis is fine-grained and table-driven (most sides are a union of
classes, not one "word-ness" bit). Our ~Boundary~ enum's bug is
checking the wrong pair (neighbor *and* the rule's own edge character)
instead of the neighbor alone.

An enhanced ~Boundary~ checking only the real neighbor would be
sufficient — and structurally identical to the ~ClassConstraint~/
~StartClass~/~EndClass~ mechanism already used for ~before~/~after~
(the "Handle word boundaries" ADR already flags this reuse). The one
gap shared by both mechanisms today: most opcodes need a class *union*
(~Space,Punctuation~ etc.), which neither expresses directly —
~Boundary~ hardcodes one variant per union, ~ClassConstraint~ only
ANDs.

~midnum~ and ~endnum~ share the same bug. It only manifests when the
matched rule's own text shares the class being tested for.

~begnum~ already sidesteps half the problem: its "before" side uses
~Boundary::AfterSpaceOrPunct~, a genuine lookbehind-only check on the
real neighbor that never touches ~c~.

Confirmed which opcodes our ~Boundary~ mechanism is actually wired to
(by reading every ~insert()~ call in ~primary.rs~): twelve of the
thirteen above (~decpoint~ excepted, per the note), plus four more —

~joinword~, ~lowword~, ~prepunc~ and ~postpunc~ are implemented in
liblouis via a *scan* (~onlyLettersAhead~, an outward scan) and we
currently approximate with a plain ~Boundary~ check. No
single-neighbor class lookup expresses a scan — these four need their
own follow-up.

** Decision Drivers

- Don't maintain two differently-coded mechanisms for the same
  non-consuming-lookaround idea
- Fix the actual bug (wrong pair of characters checked) *and* the
  per-opcode set precision, not just the one opcode with a failing
  test
- Minimize regression risk: most passing tests exercise letter-based
  rules
- Reuse ~ClassConstraint~ if possible
- Don't conflate this with the four scan-based opcodes
  (~joinword~/~lowword~/~prepunc~/~postpunc~), which need a different
  fix entirely

** Considered Options

1. *Narrow fix only* — fix ~midword~ alone via ad hoc
   ~class_constraints~, leave ~Boundary~ and the rest of the family
   as-is. Minimal and safe; leaves the same bug live in the other
   eleven single-neighbor opcodes.

2. *Fix the whole single-neighbor family* — re-express all twelve
   opcodes above as ~ClassConstraint~s with their correct sets,
   retiring ~is_word_start~/~is_word_end~/~is_number_start~/
   ~is_number_end~ for these opcodes. Needs class-union support
   first. The four scan-based opcodes are untouched either way.

3. *Full consolidation* — add class-union support, re-express every
   remaining ~Boundary~ variant (Punctuation/WordPunctuation/
   PunctuationWord, used by the scan-based opcodes) as
   class-constraints too, delete ~Boundary~ entirely. Doesn't
   actually fix the scan-based opcodes' real gap (they'd still be
   approximations, just with a different-shaped primitive
   underneath) — so this buys mechanism uniformity, not correctness,
   for those four.

4. *Status quo* — no change beyond Bug B's narrow fix; leave the
   wider question open.

** Decision Outcome

Chosen option: *2*. ~Boundary~ and every
~is_word_start~/~is_word_end~-family predicate are deleted;
~Transition::StartClass~/~EndClass~ were generalized in place to
~Start~/~End(Vec<CharacterClass>)~, giving class-union support via
~.any()~ across the vec — the same type now serves both the
opcode-intrinsic checks in the table above and the ~before~/~after~
keyword, closing the "two mechanisms" gap this ADR was written about.

One subtlety surfaced only during implementation, not analysis: a
missing neighbor (true start/end of input) must satisfy exactly the
~Space~ requirement directly (~*class == CharacterClass::Space~ on
~None~), not a substituted placeholder character.

Confirms option 3's premise directly: deleting ~Boundary~ for the
scan-based opcodes bought nothing for them. Re-pointing ~prepunc~/
~postpunc~ at the new ~Vec<CharacterClass>~ mechanism (still a
~Start[Space]~/~End[Space]~ approximation, no scan) made their
approximation *worse* than the old ~Boundary~-based one. Decided to
accept this rather than block the rest of the fix on it: the test is
disabled rather than asserting the wrong behavior. The real fix,
liblouis's actual scan, is tracked as a follow-up.

*Follow-up implemented:* ~prepunc~/~postpunc~ now get liblouis's real
scan via a dedicated ~PunctuationConstrainer~, computed once per input
like ~HyphenationConstrainer~ — not a trie transition at all, since
the scan is variable-length (forward/backward past further
punctuation to a letter or digit, stopping at whitespace), which
~Transition::Start~/~End~ structurally can't express. The trie insert
for both opcodes now carries no boundary; eligibility
is gated post-hoc in ~trace()~ by matching the candidate's origin rule
against ~constraints.prepunc_ok_at()~/~postpunc_ok_at()~.

** Links

- [[https://github.com/liblouis/liblouis/blob/master/liblouis/lou_translateString.c][liblouis lou_translateString.c]] — ~for_selectRule~ in full: the
  generic ~before~/~after~/~nocross~ layer, then the per-opcode
  switch (single-neighbor checks, the scan-based opcodes'
  ~onlyLettersAhead~/~onlyLettersBehind~, ~largesign~'s
  reject-or-reclassify pattern, ~match~/~context~'s own pattern
  language, ~repword~'s repetition algorithm) — surveyed in full to
  answer "is it always a before/after class check" (no)
- ~src/translator/trie.rs~ — ~Transition::Start~/~End(Vec<CharacterClass>)~,
  ~ClassConstraint~ (~Boundary~ and the ~is_word_start~/~is_word_end~
  family are deleted as of ~96035dd~, not just superseded)
- ~src/translator/table/primary.rs~ — every word/number opcode's
  ~insert()~ call now uses the table above directly
- ~src/translator/position_constraints.rs~ — ~PunctuationConstrainer~,
  implementing the follow-up scan described above
- ~src/translator/table/primary.rs~ — ~prepunc_postpunc~ test,
  re-enabled with the follow-up fix
- [[*ACCEPTED Handle word boundaries][ADR: Handle word boundaries]] — chose non-consuming trie
  transitions, already noting the ~before~/~after~ generalization
  this ADR proposes finishing
- Related to :: the capsletter/begcapsword ADR above — a second case
  this session of liblouis being cleaner than our encoding of it

* DRAFT Normalize input text and translation rules to a canonical Unicode form
- Deciders :: CE
- Date :: [2026-06-23 Di]

** Context and Problem Statement

Unicode text can represent the same character in multiple equivalent
forms. For example, the Portuguese letter ~à~ can be encoded as a
single precomposed code point (U+00E0, NFC form) or as a base letter
~a~ followed by a combining grave accent (U+0061 U+0300, NFD form).
When input and table rules use different forms, translation fails:
the rule for the precomposed character does not match the decomposed
input, producing garbage output.

This problem is tracked in [[https://github.com/liblouis/liblouis/issues/98][liblouis issue #98]] and has been known since
at least 2015.

A first attempt at a solution normalized input to NFC at the pipeline
boundary. This fixed the Portuguese grade-1 table (pt-pt-g1.utb),
which defines rules for precomposed characters. However, it broke
tables that deliberately define rules for standalone combining
characters:

- IPA (~IPA-unicode-range.uti~) defines ~sign \x0302 4-146~ (combining
  circumflex = falling tone mark). After NFC, ~e + U+0302~ becomes
  ~ê~ (U+00EA); the combining-mark rule can never fire.
- Similarly for Hebrew (~hbo.yaml~), Yiddish (~yi.yaml~), and Greek
  (~el-forward.yaml~).

The Portuguese table alone needed 63 explicit ~correct~ rules for
NFD → NFC conversion in its computer-braille variant; the grade-1
variant had none and was silently broken for NFD input.

** Decision Drivers

- Table authors should write rules in a single, canonical form and not
  need to worry about which normalization form their input will arrive in
- Users should not need to know about NFC vs NFD when providing input
- louis-rs aims for a cleaner, more predictable contract than liblouis
- The solution must work for both classes of table:
  1. Tables that define rules for precomposed characters (e.g. ~letter á~)
  2. Tables that define rules for standalone combining characters (e.g. ~sign \x0302~)
- Braille codepoints (U+2800–U+28FF) have no canonical decompositions,
  so the choice of normalization form is a no-op for backward translation

** Considered Options

1. *NFC (Canonical Composition)*: normalize
   both input and rules to NFC. Precomposed forms win. Fixes class-1
   tables (precomposed rules, NFD input), breaks class-2 tables
   (standalone combining-mark rules).

2. *NFD (Canonical Decomposition)*: normalize both input and rules
   to NFD. Every precomposed character is decomposed into base + combining
   sequence. A table rule for ~á~ (U+00E1) is stored in the trie as the
   two-character path ~a + U+0301~; input ~á~ or ~a + U+0301~ both
   decompose to ~a + U+0301~ and follow the same path. A table rule for
   standalone ~\x0302~ is already in NFD form (a lone combining mark has
   no canonical decomposition). Input ~e + U+0302~ stays as-is; ~e~
   matches the ~e~ rule, ~U+0302~ matches the combining-mark rule. Input
   ~ê~ decomposes to ~e + U+0302~ and is handled the same way.

   NFD therefore appears to satisfy both classes of table simultaneously.
   It has not yet been implemented and corner cases (word-boundary
   detection across grapheme-cluster splits, case-insensitive matching
   with decomposed characters) have not been fully evaluated.

3. *Leave normalization to the caller* (the liblouis approach):
   no normalization at the library level. Table authors add explicit
   ~correct~ rules for each NFD sequence they expect. This is verbose
   (63 rules for one Portuguese variant) and error-prone — tables that
   omit the rules silently produce garbage output.

4. *Per-table opt-in*: a table metadata directive (~#-unicode-form:~)
   selects the normalization form per table. Flexible but shifts
   complexity to table authors.

** Decision Outcome

Not currently implemented. Option 1 (NFC) was tried first —
provisionally, pending investigation of option 2 (NFD) — but was fully
reverted shortly after (commits e2a216c, then 1680744 dropping the now
unused ~unicode-normalization~ dependency): it broke every table that
defines rules for standalone combining characters (IPA, Hebrew,
Yiddish, Greek), and normalizing rule patterns without also
normalizing input (or vice versa) would have broken trie matching
outright, so both had to go together. louis-rs is currently back to
option 3 — liblouis's own status quo of no normalization at either
input or rule-pattern level — with option 2 (NFD) still open and
unimplemented.

Unlike option 3's downside (table authors must write explicit
~correct~ rules to bridge NFD input, verbose but always possible — see
Portuguese's 63 rules), NFC's downside was not something a table could
work around: a rule for a standalone combining mark has no precomposed
form to fall back to for arbitrary base characters, so those tables
were left with no fix available from the table side. That asymmetry,
not the raw pass-rate table below, is why NFC was reverted rather than
kept despite its net-positive weighted test count.

We had specifically used NFC (not NFKC). NFKC performs additional
compatibility decompositions (e.g. ligature ~ĳ~ → ~ij~) that change
character counts more aggressively and are outside the scope of what
table authors expect. This is still the right choice /if/ NFC or NFD
is revisited.

Liblouis chose option 3 for two reasons: the original author wanted
liblouis to be self-contained with no external dependencies, using gnulib
for utility code; and normalization changes character counts for decomposed
input (~a\x0300~ = 2 chars → ~à~ = 1 char), which breaks cursor-position
mappings. In Rust, the ~unicode-normalization~ crate makes this trivial,
and louis-rs does not currently implement position mapping.

** Positive Consequences

None currently realized — NFC was reverted (see Decision Outcome). Kept
here as the case for revisiting NFC/NFD:

- Users can supply text in any Unicode normalization form
- Table authors write rules in a single canonical form
- Normalization ~correct~ rules in tables become unnecessary
- The ~#-unicode-form:nfc~ metadata hint becomes unnecessary

** Negative Consequences (of the reverted NFC implementation)

- Translation rules that explicitly target isolated combining diacritics
  will never fire. NFC canonical ordering reorders sequences of combining
  marks by their Canonical Combining Class values, so input that was
  written expecting a specific ordering is silently mis-translated.
  Measured impact against the liblouis test suite (713,827 tests total):

  | Table                | Before NFC | With NFC | Change | Tests |
  |----------------------+------------+----------+--------+-------|
  | ~ipa.yaml~           |     100.0% |    76.5% | −23.5% |    34 |
  | ~yi.yaml~            |      93.8% |    68.8% | −25.0% |    16 |
  | ~ar-ar-comp8.yaml~   |     100.0% |    95.8% |  −4.2% |    24 |
  | ~el-forward.yaml~    |      84.9% |    82.8% |  −2.1% |    93 |
  | ~hbo.yaml~           |      43.7% |    41.8% |  −1.9% |   469 |
  | ~bn.yaml~            |      36.8% |    35.6% |  −1.2% |    87 |
  | ~vi.yaml~ (improved) |      47.6% |    53.7% |  +6.1% |    82 |
  | ~pt.yaml~ (improved) |      84.8% |    87.0% |  +2.2% | 1163  |

  NFC is a net gain on the weighted total (~pt.yaml~ alone has 1163
  tests), but it systematically breaks every table that uses standalone
  combining marks.

- No warning is currently emitted when a combining-mark rule is silently
  unreachable (tracked as TODO in ~trie.rs~).

** Future Considerations

- Evaluate NFD as a drop-in replacement for NFC in both ~pipeline.rs~ and
  ~trie.rs~. Two open issues must be resolved first:

  1. *Word-boundary detection at decomposed-sequence seams.* Under NFD,
     a precomposed letter like ~á~ (U+00E1) is split into two codepoints:
     base ~a~ + combining mark U+0301. After the trie consumes both
     codepoints and returns the translation, the pipeline's ~prev~ pointer
     is left at U+0301 — a combining mark, which is not a letter. When
     the next character (say ~r~ in "árbol") is looked up, any
     ~Start(Word)~ boundary check sees ~prev~ = U+0301 (non-letter) and
     ~c~ = ~r~ (letter), and incorrectly fires a word-boundary in the
     middle of the word. Under NFC, ~prev~ after translating ~á~ is the
     precomposed letter ~á~ itself, so no false boundary fires. The fix
     is to advance ~prev~ by grapheme cluster rather than by codepoint,
     but this logic does not currently exist.

  2. *Case-insensitive trie matching with decomposed uppercase.* The trie
     normalises character transitions to lowercase via ~char::to_lowercase~.
     Under NFD, uppercase ~Á~ decomposes to ~A~ + U+0301. ~A.to_lowercase()~
     = ~a~, so the base is handled correctly, but it is unclear whether
     multi-codepoint uppercase sequences (some scripts produce more than one
     codepoint from ~to_lowercase~) are handled correctly in all cases.
- If position mapping (cursors/typeforms) is added, the normalization step
  must also produce a character-index mapping so callers can relate
  positions in the original input to positions in the normalized input.
  NVDA solved this (see [[https://github.com/nvaccess/nvda/pull/16521][nvaccess/nvda#16521]]).

** Links

- [[https://github.com/liblouis/liblouis/issues/98][liblouis issue #98: glyph vs diaeresis]]
- [[https://github.com/nvaccess/nvda/pull/16521][nvaccess/nvda#16521: normalization mapping in NVDA]]
- [[https://unicode.org/reports/tr15/][Unicode Standard Annex #15: Unicode Normalization Forms]]
- Commit e2a216c — reverted NFC normalization from ~pipeline.rs~ and
  ~trie.rs~
- Commit 1680744 — dropped the then-unused ~unicode-normalization~
  dependency

* DRAFT Match-rule candidate selection: weight vs. table-definition order
- Deciders :: CE
- Date :: [2026-07-04 Sa]

** Context and Problem Statement

Our engine selects among competing ~match~-rule candidates at a given
position by collecting every rule that matches there and picking the
one with the highest ~weight()~ (total characters checked, including
~pre~/~post~ context — see ~primary.rs~'s ~max_by_key~). liblouis does
something different: ~for_selectRule~ walks a hash bucket of candidate
rules in table-definition order and returns on the *first* one that
validates (~lou_translateString.c~, the ~CTO_Match~ case ends in a
bare ~return;~) — it never considers the rest of the bucket once one
rule succeeds.

These two strategies usually agree, but diverge when a well-scoped
rule and a rule whose ~pre~/~post~ can reach arbitrarily far outward
both match the same position. Concretely, in ~en-ueb-g2.ctb~:

#+begin_example
match !%[^_~] ing - 346          # "ing" at the true end of a word
match %[^_]!%[_1]* in !%[_1]*%[^_] 35   # "in" surrounded by boundary-or-more-letters
#+end_example

For "abandoning", liblouis tries the "ing" rule first (it's defined
earlier), it validates, and liblouis stops there. Our engine instead
discovers *both* candidates — the "in" rule's ~pre~ can walk backward
through the whole of "abandon" (all ordinary letters) to reach a
boundary at the true start of input, giving it a much larger weight
than "ing"'s — and picks "in", producing "abandon" + "in" + "g"
instead of the correct "abandon" + "ing".

Worth being explicit about what kind of fix this is. liblouis's own
first-match-by-definition-order mechanism is genuinely fragile:
nothing in the rule syntax declares "this beats that rule", it's
implicit in table position (and in which hash bucket a rule lands in,
which table authors don't directly control either) — reordering two
rules, or a table merge that shuffles definitions, can silently
change output with no warning. That's a real liblouis design smell.

Crucially, though, this project's remit is not limited to "replicate
liblouis's behavior, however fragile" — louis-rs's ambition includes
being a *better, more logical* implementation. That reopens an option a
strict-compatibility project wouldn't have: fixing the *rule* so it
is unambiguous by construction (e.g. giving the "in" rule an explicit
"not followed by g" exclusion), which fixes both engines at once and
is a more honest expression of what the rule was trying to say in
the first place, rather than reproducing liblouis's order-dependent
tie-break as a permanent feature of louis-rs.

That said, upstream table changes have a much bigger blast radius than
an engine-side fix confined to this repo — they affect every liblouis
consumer, not just louis-rs, and go through liblouis's own review
process on its own timeline. Whether and when to pursue that is the
maintainer's call, not a default.

** Decision Drivers

- For options 1–3 (the engine keeps behaving the same for a given
  table), matching liblouis's actual output takes priority over how
  "clever" or general our own selection heuristic is — cleverness
  that diverges from liblouis's real behavior is a bug there, not an
  improvement. Option 4 is the deliberate exception: it changes what
  the table itself considers correct, so it isn't judged against
  liblouis's current output at all
- Whatever engine-side fix is chosen must not regress the (large) set
  of cases where weight-based selection already agrees with liblouis
- Precedence/definition-order is already a first-class concept
  elsewhere in the codebase (trie rule precedence, the
  ~backwards_compatibility~ feature flag) — reusing rather than
  duplicating that mechanism would be preferable if it fits here
- Fixing an ambiguous rule upstream is preferable to reproducing its
  ambiguity when it's genuinely a design smell (not just an engine
  gap) and the scope/timing works for liblouis's own release process

** Considered Options

1. *Leave weight-based selection as-is* — accept the mismatch as a
   known gap for the (so far rare) cases it affects.
2. *Definition-order tie-break* — when multiple match candidates
   apply at a position, prefer the one defined earliest in the table,
   falling back to weight only when order is somehow unavailable/tied.
3. *Full first-match-wins* — replicate liblouis's behavior exactly:
   stop at the first candidate (in definition order) that validates,
   never comparing weight at all. Would need care to confirm this
   doesn't regress cases that currently work by picking the
   *longest* of several validating candidates.
4. *Fix the ambiguous rule(s) upstream* — e.g. scope the "in" rule so
   it cannot match the tail of "abandoning" at all, removing the
   collision at the source instead of arbitrating it in either
   engine. Doesn't need to be exclusive of 1–3: even with this
   pursued for known cases, the engine still needs *some* selection
   behavior for tables that haven't been (or won't be) revisited.
   Requires first assessing how common this class of ambiguity is
   across the shipped tables, to judge whether it's a one-off or a
   pattern worth addressing systematically.

   Prototyped for the "in"/"ing" case specifically (not yet committed
   upstream): give "in" a new numbered class excluding a following
   ~g~/~G~, so it can no longer match the start of an "-ing" suffix —

   #+begin_example
   attribute 3 Gg
   match %[^_]!%[_1]* in %[^_]|!%[_13]!%[_1]*%[^_] 35
   #+end_example

   Verified against a real liblouis build: identical output on the
   original collision words plus edge cases (standalone "in" before
   punctuation, "begin", "ring"/"wing"/"bring"/"sing" — the same "in"
   + "g" collision recurs there too), and zero regressions across
   every yaml test exercising ~en-ueb-g2.ctb~ (~112k tests).

   Two liblouis pattern-language traps surfaced while getting this
   right, worth remembering for any future table-level fix: wrapping
   a negated check in ~(...)?~ is a no-op (a failed negation is just
   skipped, and the following token often accepts the same character
   anyway); and ~!(A|B)~ does *not* De Morgan into "matches neither" —
   liblouis's alternation ORs the two negated branches instead of
   ANDing them, so it degenerates to "matches almost anything" whenever
   the two branches are mutually exclusive. The same mistake already
   exists unnoticed in ~afr-za-g2.ctb:229~. Combining an attribute-set
   exclusion with a literal-character exclusion into one real check
   only works by folding both into a single numbered class.

** Decision Outcome

The general engine-side question (options 1–3) is still undecided —
no change has been made to ~match_pattern.rs~ or the candidate
selection logic. A first attempt at fixing the underlying
~BoundaryMode::AssumeAtStart~ bug (option: just let non-boundary
checks match normally, same as ~Otherwise~) is correctly targeted for
that bug in isolation, but caused a net regression on the full test
corpus (94.9% → 94.4% total, 93.9% → 91.8% on
~en-ueb-g2-dictionary_harness.yaml~) once it made rules like the "in"
one above newly reachable at position 0. That fix was reverted rather
than shipped. Revisiting it requires deciding among the options above
first.

Option 4 has been carried out for this one concrete collision (see
prototype above) and left sitting uncommitted in the local liblouis
checkout — pushing it upstream is the co-maintainer's call on timing,
not done as part of this work. It removes the "abandoning" case from
being a live example of the mismatch, but the underlying engine
question remains open for every other table that hasn't been (and may
never be) similarly revisited.

** Links

- ~TODO.org~ — "Word/group-sign contraction" entry under Known gaps
  for the concrete repro and regression numbers
- ~src/translator/table/primary.rs~ — ~match_candidates~,
  ~context_candidates~, weight-based candidate selection
- ~src/translator/match_pattern.rs~ — ~BoundaryMode::AssumeAtStart~

* ACCEPTED Pre-compute indication events before translation
- Deciders :: CE
- Date :: [2026-06-23 Di]
- Implemented :: [2026-06-26 Do]

** Context and Problem Statement

Braille indication — the insertion of indicator cells to signal
numeric sequences, uppercase runs, letter signs, and emphasis — is
currently implemented as a set of mutable state machines
(~numeric::Indicator~, ~uppercase::Indicator~, ~lettersign::Indicator~,
~nocontract::Indicator~) that walk the input in parallel with the
translation loop. At each position the translation loop calls
~indicators.next()~, which advances all state machines and returns any
indication translations to emit.

This works for built-in indication, but two problems remain unsolved:

1. *Typeform integration.* Users can supply a per-character ~typeforms~
   array annotating emphasis (italic, bold, …). These annotations also
   require start/end indicators in the braille output, but the typeform
   array is entirely separate from the indicator state machines. There
   is no clean mechanism to merge the two sources of indication.

2. *Awkward mutable clone.* The translation table (~PrimaryTable~) is
   intended to be immutable and shared, but the indicators carry mutable
   state, so they must be cloned for every translation call (see the
   FIXME at ~primary.rs:722~). This is a structural smell.

The question is whether indication events can be *pre-computed* from
the input text before the translation loop begins, producing an
immutable per-position event array that both the built-in state
machines and the typeform array populate, and that the translation loop
consults by simple indexed lookup.

*** Does indicator state affect rule selection?

This is the key correctness concern: if which rules fire depends on
indicator state, pre-computing events before translation may produce a
different answer than computing them on the fly.

Investigation of the liblouis C source (~lou_translateString.c~) reveals
that indicator state *does* influence rule selection via a ~dontContract~
flag:

- ~checkNumericMode()~ is called at every position before
  ~for_selectRule()~. When it detects entry into numeric mode (a digit
  or ~numericmodechars~ character followed by a digit) it sets both
  ~numericMode = 1~ and, when the table's ~noContractSign~ equals its
  ~noNumberSign~, ~dontContract = 1~.
- ~dontContract~ is passed into ~for_selectRule()~ and suppresses about
  15 contraction-type rules (~CTO_Always~, ~CTO_WholeWord~,
  ~CTO_BegWord~, ~CTO_EndWord~, ~CTO_Match~, and others).
- The ~no_contract~ bit in the user-supplied ~typebuf~ (typeforms) also
  sets ~dontContract~ directly (~lou_translateString.c:3684~).

Crucially, both the indication emission (numsign) and the
contraction-suppression (~dontContract~) are computed from the *same
source*: character class membership of the input characters (~CTC_Digit~,
~CTC_LitDigit~, ~CTC_NumericMode~, ~CTC_NumericNoContract~). Neither
depends on what braille has already been output. The two effects happen
together as consequences of the same input-character analysis, not as a
causal chain where emitting the indicator changes subsequent rule
selection.

Therefore both the indication events and the ~dontContract~ spans are
*pre-computable* from the input text and the character class
definitions.

*** The ~prevTransOpcode~ / ~exactdots~ edge case

~for_selectRule()~ also checks ~prevTransOpcode != CTO_ExactDots~ before
accepting ~CTO_MidNum~ and ~CTO_EndNum~ rules. This looks like a genuine
translation-decision dependency. However:

- ~exactdots~ is an opcode intended exclusively for
  [[https://github.com/liblouis/liblouisutdml][liblouisutdml]] semantic-action files, which inject raw dot patterns
  into a translation mid-stream via ~@dots~ sequences in the source
  document.
- No standard braille table shipped with liblouis uses ~exactdots~.
- Louis-rs operates on plain text input with table-driven translation;
  there is no mechanism to inject raw dot patterns mid-stream.

The ~prevTransOpcode == CTO_ExactDots~ guard is therefore outside the
scope of louis-rs and can be disregarded for this decision.

** Decision Drivers

- Built-in indication (numeric, uppercase, lettersign, nocontract) and
  user-supplied typeform indication are the same phenomenon and should
  be handled by the same mechanism
- The mutable-clone pattern for indicators should be eliminated
- The translation loop should be simplified: indication should not
  require threaded mutable state alongside rule selection
- Contraction suppression in numeric mode (~dontContract~) must remain
  correct under the new design
- The data structure should be cache-friendly (no heap allocation per
  character position)

** Considered Options

1. *Status quo with typeform state machine added* — keep the existing
   mutable state machines, add a parallel state machine over the
   typeform array, merge outputs at each position in the loop.

2. *Pre-computed events array* — run all state machines (and the
   typeform array) as a single preprocessing pass before translation
   begins, producing an immutable ~Vec<IndicationFlags>~ indexed by
   character position. The translation loop consults this array by index.

** Decision Outcome

Chosen option: *Pre-computed events array* (option 2).

The preprocessing pass runs each indicator state machine and the
typeform array once over the input string, recording at each character
position a bitflag word that encodes:

- Which indication events fire at this position (emit numsign, emit
  capsletter, emit lettersign, emit emphasis start/end, …)
- Whether contraction is suppressed at this position (~dontContract~
  equivalent, derived from numeric mode and the ~no_contract~ typeform
  bit)

The translation loop replaces ~indicators.next(chars.as_str(), prev)~
with a single array lookup. The indicators no longer need to be cloned;
the array is immutable and cheaply shareable.

The event type is defined in ~src/translator/indication/events.rs~.
Each position uses ~EnumSet<IndicationEvent>~ (from the ~enumset~ crate),
which compiles to a compact integer with no per-position heap allocation.
~Indicators::precompute()~ returns a ~PrecomputedIndications~ value that
bundles the per-position event array with the event-to-translation map;
callers ask for ~translations_at(pos)~ directly.

** Positive Consequences

- Typeforms and built-in indication are unified: both populate the same
  array during preprocessing, both are consumed identically in the
  translation loop.
- The mutable-clone FIXME in ~primary.rs~ is eliminated; ~PrimaryTable~
  becomes fully immutable.
- The translation loop is simpler: one array lookup per position instead
  of calls into multiple state machines.
- Indication logic is testable in isolation: the preprocessing pass can
  be unit-tested independently of the translation loop.
- Contraction suppression (~dontContract~) is also pre-computed,
  keeping the liblouis-equivalent behavior correct without live state in
  the loop.

** Negative Consequences

- An additional allocation proportional to input length is required
  before translation begins. For typical inputs this is negligible.
- The preprocessing pass is a second traversal of the input. This is
  also negligible compared to the translation work, but it is a
  structural change that new contributors will need to understand.
- If a future liblouis feature introduces genuine feedback from
  translation decisions back into indication state (analogous to
  ~prevTransOpcode~ for ~midnum~, but without the ~exactdots~ escape
  hatch), the pre-computation assumption would need to be revisited.

** Pros and Cons of the Options

*** Status quo with typeform state machine added

- Good, because it is a minimal change and does not restructure the
  translation loop
- Bad, because it adds yet another parallel mutable iterator to an
  already crowded loop
- Bad, because built-in and typeform indication remain structurally
  separate with no shared abstraction
- Bad, because the mutable-clone pattern remains

*** Pre-computed events array

- Good, because it unifies built-in and typeform indication under one
  abstraction
- Good, because it eliminates mutable state from the translation hot loop
- Good, because the preprocessing step is independently testable
- Good, because the bitflag representation is cache-friendly with no
  per-position heap allocation
- Bad, because it requires a dedicated preprocessing pass and the
  associated ~Vec~ allocation

** Links

- ~src/translator/indication/events.rs~ — ~IndicationEvent~ enum and ~IndicationEvents~ type
- ~src/translator/indication.rs~ — ~PrecomputedIndications~, ~Indicators::precompute()~, ~From<TextAttribute>~ and ~From<&[TextAttributes]>~ bridge impls
- ~src/translator/indication/numeric.rs~ — numeric indicator ~precompute()~
- ~src/translator/indication/uppercase.rs~ — uppercase indicator ~precompute()~
- ~src/text_attribute.rs~ — ~TextAttribute~ enum (what liblouis calls typeform)
- [[https://github.com/liblouis/liblouis/blob/master/liblouis/lou_translateString.c][liblouis lou_translateString.c]] — authoritative reference for
  ~checkNumericMode~, ~dontContract~, and ~for_selectRule~

* ACCEPTED Backward-translation indication with pre-computed events
- Deciders :: CE
- Date :: [2026-06-26 Do]

** Context and Problem Statement

Forward-translation indication is now pre-computed: each indicator runs
a single pass over the text input and records, at each character
position, which events fire there. The translation loop consults an
immutable ~PrecomputedIndications~ array instead of advancing mutable
state machines in parallel.

Backward translation (braille → text) poses the symmetric but inverted
problem. In forward translation the indicators are *inserted* — the
state machines detect conditions in the text and emit braille indicator
cells. In backward translation the indicator cells are already present
in the braille input and must be *detected* and *consumed*, with their
meaning applied to how subsequent braille cells are interpreted.

The question is how to extend the pre-computed architecture to cover
backward indication, and in particular whether the same ~precompute()~
interface and ~PrecomputedIndications~ type can be reused or whether a
separate abstraction is needed.

*** Detection vs insertion

In forward translation, ~numsign~ means: /when the input text enters a
numeric run, emit the numsign dot pattern before the first digit./
In backward translation, ~numsign~ means: /when the braille input
contains the numsign dot pattern, the following cells represent digits./

The indicator cell itself has no text output — it is consumed and
suppressed — but it sets a mode that affects how subsequent braille
cells are matched against back-translation rules. This is analogous to
the forward ~DontContract~ flag, but inverted: instead of suppressing
contractions in the *source*, it forces digit interpretation in the
*target*.

*** Same opcode, inverted state machine

The same table opcode (~numsign~, ~capsletter~, ~begcapsword~, …) drives
both directions. In forward translation the state machine watches for
triggering characters in the text and emits the indicator cell. In
backward translation the state machine watches for the indicator dot
pattern in the braille input and sets a mode flag for subsequent cells.

The key question for pre-computation: can the backward state machine
also analyse the entire braille input in one pass before the translation
loop, producing a per-position array of interpretation flags?

*** Does the output affect subsequent indicator detection?

In forward translation, pre-computation is valid because the indicator
events depend only on the input text characters, not on what braille has
already been emitted. The analogous question for backward translation is
whether the detection of indicator cells in the braille input depends on
what text has already been output.

For numeric indication this is straightforward: the numsign dot pattern
is a fixed, recognisable cell. Its detection depends only on the braille
input sequence, not on the output text produced so far.

For uppercase indication the picture is similar: ~capsletter~ and
~begcapsword~ are fixed dot patterns. Their detection is independent of
prior output.

However, unlike forward translation where we know the exact character
boundaries of the input text, braille back-translation operates on a
sequence of braille cells whose correspondence to output characters is
determined by the rule matching. This means a braille-position index is
not the same as a text-character index, and the two cannot be trivially
unified.

*** Implication for rule selection

In forward translation, the ~DontContract~ event pre-computed at a text
position suppresses contraction rules when that position is translated.
In backward translation, a detected ~numsign~ suppresses non-digit
back-translation rules for the following braille cells. The mechanism
is the same in structure — a per-position flag that modulates rule
selection — but the positions are braille-cell positions rather than
text-character positions.

** Decision Drivers

- The pre-computed architecture should extend naturally to backward
  translation rather than reverting to the mutable-state-machine pattern
- The correspondence between braille-cell positions and output-text
  positions must be handled consistently
- The same ~IndicationEvent~ vocabulary should be reusable where possible
- Backward indicator detection must remain correct even for tables that
  define multi-cell indicator patterns

** Considered Options

1. *Separate backward precompute pass over braille input* — analogous to
   the forward pass but running over braille cells. Produces a
   ~PrecomputedIndications~ indexed by braille-cell position. The
   backward translation loop consults it by braille-cell index.

2. *Inline detection during backward translation* — detect indicator
   cells opportunistically as the backward loop encounters them, without
   a separate pass. Avoids the braille-position / text-position alignment
   problem but reintroduces mutable state in the translation loop.

3. *Reuse forward precompute on the back-translated output* — run the
   forward indicator state machines on the *reconstructed text* after
   a first-pass back-translation, then use the results to refine a
   second pass. Correct in principle but doubles the translation work
   and introduces a two-pass architecture.

** Decision Outcome

Chosen option: *Separate backward precompute pass over braille input*
(option 1).

The alignment worry — that braille-cell positions and output-character
positions don't correspond one-to-one — turned out not to block
pre-computation, because detection only ever needs the braille-cell
index: the pass walks the braille input once and records, per cell,
which interpretation constraints apply (force uppercase, force digit
reading, etc.). The translation loop still resolves cells to text via
normal rule matching; it just consults the constraint at each cell's
position along the way, the same way forward translation consults its
precomputed events at each text position.

In the process this generalized further than either ADR anticipated:
forward and backward indication now share a single per-position
constraint mechanism, rather than a ~PrecomputedIndications~-shaped
type on one side and something bespoke on the other. Numeric-mode
suppression, computer-braille spans, and the backward caps/digit
disambiguation this ADR was written for are all instances of the same
idea — a set of constraints attached to a position, computed once
before translation, consulted read-only during it.

** Links

- [[*ACCEPTED Pre-compute indication events before translation][ADR: Pre-compute indication events before translation]] — the forward
  architecture this ADR extends
- ~src/translator/position_constraints.rs~ — the shared per-position
  constraint mechanism both directions now use
- [[https://github.com/liblouis/liblouis/blob/master/liblouis/lou_backTranslateString.c][liblouis lou_backTranslateString.c]] — authoritative reference for
  backward indication handling

* SUPERSEDED Regexps duplicate character transitions
- Deciders :: CE
- Date :: [2024-12-24 Di]

** Context and Problem Statement
We have a graph for all regular expressions. For alternatives we
create a new node and add all the alternatives between the current and
the new node. Now what do we do if there is already a character
transition for one of the characters in the alternatives? We could
either
1. merge the nodes
   - how do we handle the case where both nodes are accepting, i.e.
     have translations? Which translation gets precedence?
2. add an epsilon transition
   - from the new node to the existing one
   - and delete the edge
3. allow multiple character transition edges with the same character
   from the same node
   - this of course totally breaks the current data structure

#+begin_example
letter a 1
letter b 2
letter o 3
letter r 4
letter z 5

always bor 17
match b(a|o) r z 14
#+end_example

#+begin_example
/bor|b(a|o)rz/
#+end_example

** Decision Drivers
** Considered Options
** Decision Outcome

Superseded — the question is moot. The regex engine was later
rewritten from a shared character-transition graph to a VM that
compiles alternation to a pair of branching instructions, each
alternative getting its own instruction stream rather than a node
other patterns might also land on. Matching runs all live branches
in parallel and asks which one finished, so two alternatives that
happen to start with the same character never need to merge, share,
or otherwise coordinate — there is no shared node for them to
collide on in the first place.

** Links

- ~src/translator/regexp.rs~ — the VM-based engine that replaced the
  graph model this ADR was written against

* ACCEPTED Hyphenation
- Deciders :: CE
- Date :: [2026-08-20 Do]

** Context and Problem Statement

Liblouis uses hyphenation dictionaries from the TeX project to provide
some functionality in the form of the ~nocross~ opcode prefix. It
would be nice if we could use off-the-shelf functionality instead of
having to re-implement this as in the C version.

The [[https://crates.io/crates/hyphenation][hyphenation crate]] makes it fairly easy to use a dictionary. It
comes pre-configured with [[https://github.com/tapeinosyne/hyphenation/tree/master/dictionaries][a lot]] of TeX and OpenOffice hyphenation
dictionaries. These come not in their standard form but are encoded
using the bincode format. This encoding happens during the build
process of the hyphenation crate, where all the [[https://github.com/tapeinosyne/hyphenation/tree/master/patterns][pattern files]] in
the ~patterns~ directory are ecoded and stored in the ~dictionaries~
directory.

I added the 3 relevant dictionaries from liblouis, namely
~da-dk-g2.dic~, ~de-g1-core-patterns.dic~ and
~de-g2-core-patterns.dic~ to the patterns folder of hyphenation, added
the files to ~build.rs~ and ~hyphenation_commons/src/language.rs~ and
finally built the hyphenation crate with

#+begin_src shell
  cargo build --features build_dictionaries
#+end_src

The liblouis dictionary files were encoded and I grabed them out of
~target/debug/build/hyphenation-4f7fc3b4af290d85/out/dictionaries~.

You can now load this dictionary and hyphenate words:

#+begin_src rust
  use std::error::Error;

use hyphenation::Load;
use hyphenation::{Hyphenator, Language, Standard};

fn main() -> Result<(), Box<dyn Error>> {
    let path_to_dict = "/path/to/da-g2.standard.bincode";
    let en_us = Standard::from_path(Language::Dutch, path_to_dict)?;

    let hyphenated = en_us.hyphenate("bestemmer");
    println!("Hello, {:?}!", hyphenated);

    Ok(())
}
#+end_src

which results in

#+begin_src shell
cargo run
Hello, Word { text: "bestemmer", breaks: [7] }!
#+end_src

You'll notice that I used the language ~Language::Dutch~. The
language ~DanishGrade2~, that I had added to my local version of the
~hyphenation_commons~ crate, does not exist when I use the
~hyphenation~ crate from crates.io. If I use ~Language::EnglishUS~ it
compiles but complains and tells me the the dictionary is in for the
~Language::Dutch~.

The problem is that the ~hyphenation_commons~ crate converts the list
of languages to an enum that is baked into the build. There does not
seem to be a way to load a dictionary with out the ~Language~ enum.
The bincode seems to contain the language in its serialized data
structure.

At the moment it definitely looks like there is more research needed
as to how we could use the hyphenation crate using our own
dictionaries. Maybe we'll have to rip out the relevant parsing code
from ~hyphenation_commons~ and then provide the hyphenator with a
deserialized version of that.

*** Update: the crate route was abandoned

The enum-baking problem above never got resolved cleanly, and it kept
being the wrong shape for our use case: liblouis dictionaries are
identified by table path like every other liblouis table, not by a
closed set of languages known at the crate's own build time. Since all
we actually needed from the ~hyphenation~ crate was its Knuth-Liang
pattern-matching algorithm — not its bundled TeX/OpenOffice pattern
corpus, and not its print-text case-folding — that algorithm was
reimplemented natively in ~src/hyphenation.rs~, parsing liblouis's own
~.dic~ pattern-file format directly. There is no external crate and no
build-time bincode conversion step; a hyphenation table loads and is
identified exactly like any other liblouis table.

** Decision Drivers

** Considered Options

** Decision Outcome

Chosen option: a native Knuth-Liang implementation in
~src/hyphenation.rs~, wired into table loading (~IncludeHyphenation~)
and used to filter ~nocross~ candidates: a candidate contraction is
only offered if the hyphenator does *not* find a break point inside
it.

Correctness is verified, not just integrated: the native
implementation was checked byte-identical to the old
~hyphenation~-crate-based behaviour across liblouis's full YAML test
suite (278k test cases across the en-g3 and da-dk-g26 dictionaries),
and the previously-~#[ignore]~d ~nocross~ unit test in
~primary.rs~ now passes.

** Positive Consequences

- No external dependency, no build-time bincode conversion step, and
  no crate-side enum of supported languages to patch and rebuild
  out-of-band — a hyphenation dictionary is just another liblouis
  table, identified by path.
- Verified byte-identical to the crate-based behaviour it replaced
  across liblouis's full YAML test suite, so the switch carried no
  regression risk.

** Negative Consequences

- We now own the pattern-matching algorithm ourselves instead of
  depending on a maintained crate, though it verified out at parity
  with the crate's implementation and is small in scope.
- ~nocross~'s use of hyphenation is still a coarser check than
  liblouis's: it asks whether the matched candidate substring has any
  break anywhere, rather than liblouis's positional check of whether a
  break falls inside *that candidate's span* within the enclosing
  word. This predates the native rewrite (the old
  ~hyphenation~-crate-based check was exactly as coarse) and isn't
  caused by it; tracked separately in ~TODO.org~ under "Known gaps".

** Pros and Cons of the Options

** Links

- ~TODO.org~ — "implement \"libhyphen\"", "Hyphenation", and "Known
  gaps" (the two hyphen-related entries)
- ~src/hyphenation.rs~ — ~HyphenationTable~, native pattern parser and
  matcher
- ~src/translator/table/primary.rs~ — ~IncludeHyphenation~,
  ~word_hyphenates~, ~nocross_candidates~
- Commit ed55f45 — fixed and un-ignored the ~nocross~ unit test (bad
  test fixture, not an algorithm bug)

* ACCEPTED Handle word boundaries
- Deciders :: CE
- Date :: [2024-03-08 Fr]

We need a way to handle word boundaries, as some rules only apply at
specific boundaries. Currently the trie does not account for any
boundaries.

** Context and Problem Statement

When given an input we have to look for a matching translation rule.
This is in some ways similar the process inside a regex engine.
However in our case mostly have consuming transitions between the
nodes. Only at the beginning and at the end of a match we can have
non-consuming word-boundary matches. This simplifies the process quite
a bit as in most cases we do not have to search through multiple
branches.

*** Summary of word-boundary opcodes
:PROPERTIES:
:COLUMNS: %14ITEM(Opcode) %WORDSTART(WordStart) %WORDEND(WordEnd) %PUNCSTART(PuncStart) %PUNCEND(PuncEnd) %NUMSTART(NumStart) %NUMEND(NumEnd) %CONSTRAINT(Constraint)
:WORDSTART_ALL: YES NO -
:WORDEND_ALL: YES NO -
:PUNCSTART_ALL: YES NO -
:PUNCEND_ALL: YES NO -
:NUMSTART_ALL: YES NO -
:NUMEND_ALL: YES NO -
:END:

#+BEGIN: columnview :hlines 1 :id local :skip-empty-rows t :match "/-TODO"
| Opcode          | WordStart | WordEnd | PuncStart | PuncEnd | NumStart | NumEnd | Constraint                                             |
|-----------------+-----------+---------+-----------+---------+----------+--------+--------------------------------------------------------|
| Word            | YES       | YES     |           |         |          |        | Surrounded by whitespace and/or punctuation            |
| Joinword        | YES       | YES     |           |         |          |        | Followed by whitespace and a letter                    |
| Lowword         | YES       | YES     | NO        | NO      |          |        | Surrounded by whitespace                               |
| Compbrl         | YES       | YES     |           |         |          |        | Surrounded by whitespace                               |
| Nocont          | YES       | YES     |           |         |          |        | Surrounded by whitespace                               |
| Begword         | YES       | NO      |           |         |          |        |                                                        |
| Sufword         | YES       | -       |           |         |          |        |                                                        |
| Midword         | NO        | NO      |           |         |          |        |                                                        |
| Partword        | NO        | NO      |           |         |          |        | Proceeded or followed by a letter                      |
| Midendword      | NO        | -       |           |         |          |        |                                                        |
| Prfword         | -         | YES     |           |         |          |        |                                                        |
| Endword         | -         | YES     |           |         |          |        |                                                        |
| Begmidword      | -         | NO      |           |         |          |        |                                                        |
| Replace         | -         | -       |           |         |          |        |                                                        |
| Always          | -         | -       |           |         |          |        |                                                        |
| Repeated        | -         | -       |           |         |          |        |                                                        |
| Largesign       | -         | -       |           |         |          |        |                                                        |
| Syllable        | -         | -       |           |         |          |        |                                                        |
| Contraction     | -         | -       |           |         |          |        |                                                        |
| Prepunc         | (YES)     | (YES)   | YES       |         |          |        | Part of punctuation at the beginning of a word         |
| Postpunc        | (YES)     | (YES)   |           | YES     |          |        | Part of punctuation at the end of a word               |
| Begnum          |           |         |           |         | YES      | -      | At the beginning of a number                           |
| Midnum          |           |         |           |         | NO       | NO     | In the middle of a number                              |
| Endnum          |           |         |           |         | -        | YES    | At the end of a number                                 |
| Joinnum         | -         | -       |           |         | -        | -      | If whitespace and a number follows omit the whitespace |
| TODO Rependword |           | YES     |           |         |          |        |                                                        |
#+END:

**** Word
:PROPERTIES:
:WORDSTART: YES
:WORDEND:  YES
:CONSTRAINT: Surrounded by whitespace and/or punctuation
:END:
**** Joinword
:PROPERTIES:
:WORDSTART: YES
:WORDEND:  YES
:CONSTRAINT: Followed by whitespace and a letter
:END:
**** Lowword
:PROPERTIES:
:WORDSTART: YES
:WORDEND:  YES
:CONSTRAINT: Surrounded by whitespace
:PUNCSTART: NO
:PUNCEND:  NO
:END:
**** Compbrl
:PROPERTIES:
:WORDSTART: YES
:WORDEND:  YES
:CONSTRAINT: Surrounded by whitespace
:END:
**** Nocont
:PROPERTIES:
:WORDSTART: YES
:WORDEND:  YES
:CONSTRAINT: Surrounded by whitespace
:END:
**** Begword
:PROPERTIES:
:WORDSTART: YES
:WORDEND:  NO
:END:
**** Sufword
:PROPERTIES:
:WORDSTART: YES
:WORDEND:  -
:END:
**** Midword
:PROPERTIES:
:WORDSTART: NO
:WORDEND:  NO
:END:
**** Partword
:PROPERTIES:
:WORDSTART: NO
:WORDEND:  NO
:CONSTRAINT: Proceeded or followed by a letter
:END:
**** Midendword
:PROPERTIES:
:WORDSTART: NO
:WORDEND:  -
:END:
**** Prfword
:PROPERTIES:
:WORDSTART: -
:WORDEND:  YES
:END:
**** Endword
:PROPERTIES:
:WORDSTART: -
:WORDEND:  YES
:END:
**** Begmidword
:PROPERTIES:
:WORDSTART: -
:WORDEND:  NO
:END:
**** Replace
:PROPERTIES:
:WORDSTART: -
:WORDEND:  -
:END:
**** Always
:PROPERTIES:
:WORDSTART: -
:WORDEND:  -
:END:
**** Repeated
:PROPERTIES:
:WORDSTART: -
:WORDEND:  -
:END:
**** Largesign
:PROPERTIES:
:WORDSTART: -
:WORDEND:  -
:END:
**** Syllable
:PROPERTIES:
:WORDSTART: -
:WORDEND:  -
:END:
**** Contraction
:PROPERTIES:
:WORDSTART: -
:WORDEND:  -
:END:
**** Prepunc
:PROPERTIES:
:WORDSTART: (YES)
:WORDEND:  (YES)
:PUNCSTART: YES
:CONSTRAINT: Part of punctuation at the beginning of a word
:END:
**** Postpunc
:PROPERTIES:
:WORDSTART: (YES)
:WORDEND:  (YES)
:PUNCEND:  YES
:CONSTRAINT: Part of punctuation at the end of a word
:END:
**** Begnum
:PROPERTIES:
:NUMSTART: YES
:NUMEND:   -
:CONSTRAINT: At the beginning of a number
:END:
**** Midnum
:PROPERTIES:
:NUMSTART: NO
:NUMEND:   NO
:CONSTRAINT: In the middle of a number
:END:
**** Endnum
:PROPERTIES:
:NUMSTART: -
:NUMEND:   YES
:CONSTRAINT: At the end of a number
:END:
**** Joinnum
:PROPERTIES:
:NUMSTART: -
:NUMEND:   -
:WORDSTART: -
:WORDEND:  -
:CONSTRAINT: If whitespace and a number follows omit the whitespace
:END:
**** TODO Repword
**** TODO Rependword
:PROPERTIES:
:WORDEND:  YES
:END:
**** TODO Exactdots
seems like a corner case for utdml
**** TODO Context
**** TODO Pass2
**** TODO Pass3
**** TODO Pass4
**** TODO Correct
**** TODO Match
**** TODO Literal

** Decision Drivers

- start with the simplest solution
- move as much processing from the translation to the compilation
  stage

** Considered Options

- Handle different contexts with separate tries :: Leave the trie code
  as is and handle the context using multiple tries with wrapper code
- Enhance trie with transitions :: Add transitions to the trie that
  map to non-consuming word boundaries
- Filter the candidates found in the trie :: Leave the trie as is and
  just filter the returned candidates.

** Decision Outcome

Chosen option: "Enhance trie with transitions", because it keeps a
single search structure instead of splitting rules across multiple
tries or computing candidates first and filtering them afterwards.
Boundaries are modelled as non-consuming transitions alongside the
normal character transitions, so a boundary check is just another
edge to follow during the same trie walk.

This turned out to generalize well: the same non-consuming-transition
idea was later reused for the liblouis ~before~/~after~
class-constraint keywords, which are a lookbehind/lookahead against a
character class rather than a fixed boundary condition.

*Update (2026-08-19):* the concrete ~Boundary~ enum described above
did not survive. It checked the wrong pair of characters — the
matched rule's own edge character in addition to the true neighbor —
which was harmless for letter-based rules (the gate always passed)
but wrong for punctuation. ~Boundary~ and the whole
~is_word_start~/~is_word_end~ family of predicates were deleted in
commit 96035dd; the non-consuming-transition *idea* survived and was
generalized instead into ~Transition::Start~/~End(Vec<CharacterClass>)~,
a direct lookbehind/lookahead against the real neighbor that now
serves both the opcode-intrinsic boundary checks and the
~before~/~after~ keyword through one mechanism. Separately,
~prepunc~/~postpunc~ turned out not to fit a trie transition at all —
liblouis implements them via a variable-length outward scan, not a
single-neighbor class check — and were moved to a dedicated
~PunctuationConstrainer~ checked post-hoc in ~trace()~, outside the
trie entirely. See the linked follow-up ADR for the full analysis.

** Positive Consequences

- One trie handles both boundary-constrained and unconstrained rules;
  nothing to keep in sync across parallel structures.
- The non-consuming-transition mechanism generalized to other
  lookbehind/lookahead needs (the ~before~/~after~ keywords) without
  changing the trie's overall shape.

** Negative Consequences

- All boundary conditions are checked at every character position,
  even when the current node has no boundary transitions to offer.
  This is a performance cost that has not yet been benchmarked against
  lazier evaluation.
- Non-consuming transitions make the trie a less pure prefix tree: a
  lookup can branch without consuming input, which is a bit more to
  hold in your head than plain character-by-character descent.

** Pros and Cons of the Options

***  Handle different contexts with separate tries

- Bad :: rules unconstrained by any boundary would need to be
  duplicated across every context-specific trie, or looked up
  separately and merged, reintroducing the synchronization problem
  this option was meant to avoid

***  Enhance trie with transitions

- Good :: unifies the search for candidates
- Bad :: makes the trie more complicated

***  Filter the candidates found in the trie

- Good :: No change needed in the trie
- Bad :: Processing during translation

** Links

- [[*ACCEPTED Unify word/number-boundary checks onto class-constraint lookaround][ADR: Unify word/number-boundary checks onto class-constraint lookaround]] —
  superseded ~Boundary~'s concrete implementation (deleted in
  96035dd) while keeping this ADR's non-consuming-transition idea;
  also moved ~prepunc~/~postpunc~ off the trie entirely

* ACCEPTED Hand-rolled regexp VM instead of the ~regex~/~regex-automata~ crates
- Deciders :: CE
- Date :: [2026-07-04 Sa]

** Context and Problem Statement

liblouis' ~match~, ~context~, ~correct~, and ~pass2~/~pass3~/~pass4~
opcodes use a regex-like pattern language (character classes,
alternation, quantifiers, negation, boundary anchors). Louis-rs
implements this with its own small VM-based regexp engine
(~translator/regexp.rs~) rather than the well-established, heavily
optimized ~regex~ crate or its lower-level ~regex-automata~ API. Given
how much engineering effort goes into a correct, fast regex engine,
and how much of that effort duplicates what those crates already
provide, is hand-rolling still the right call?

This was already considered once, tersely, in ~TODO.org~: "Instead of
hand-rolling a finite state machine to implement regular expressions
we should use regex_automata. — no, for now keep the hand-rolled
regexp engine, as we need to support effects and negation." This ADR
records the fuller reasoning behind that call, which surfaced again
while implementing the ~^~ boundary attribute and ~_N~ lookback for
~match~/~context~ rules (2026-07-03/04).

There is also a data point from this codebase's own history: an
earlier version of the engine ([[*Regexps duplicate character transitions][Regexps duplicate character
transitions]], superseded) shared character-transition nodes across
multiple rules in one graph, DFA-style. It was abandoned specifically
because liblouis rules aren't just "does this match" — each one
carries its own translation payload with its own precedence, and two
rules whose patterns would share a transition node in a merged graph
have no principled way to decide whose payload wins there. The fix
was giving every rule its own independent instruction stream
(branching via VM ~Split~, not shared graph nodes) rather than a
merged automaton.

** Decision Drivers

- ~correct~/~pass2~/~pass3~/~pass4~ rules can guard on, and mutate,
  numbered *pass-variables* — external, mutable state set by one
  rule's action and checked by a *different* rule's pattern later on
  (~VariableEqual~/~NotVariableEqual~, and *effects*). This isn't
  matching text against a pattern; it's an imperative condition on
  state unrelated to the string being scanned.
- Rule selection is precedence/weight-based across potentially
  thousands of independently-compiled patterns per table, some of
  which must be discovered starting at an earlier cursor position and
  held via ~delayed_translations~/~offset()~ until the cursor catches
  up (built for ~match~'s ~pre~, reused for ~context~'s "before
  bracket" checks and lookback). This doesn't correspond to anything
  in a single compiled pattern object's API.
- ~trace~ (the CLI subcommand showing exactly which rule fired at each
  translation step) depends on an inspectable instruction stream with
  attached payloads, not just a match/no-match answer.
- Negation needs to apply to arbitrary sub-patterns (attribute sets,
  groups, boundary checks), not just single characters or classes.

** Considered Options

1. *Hand-rolled VM* (status quo) — a small instruction set
   (~Char~/~Class~/~Split~/~Jump~/~Capture*~/~VariableEqual~/anchors/…)
   interpreted directly, in the style of Russ Cox's [[https://swtch.com/~rsc/regexp/regexp2.html][Regular Expression
   Matching: the Virtual Machine Approach]].
2. *~regex~ crate* — compile each rule's pattern to a ~Regex~, matching
   with the standard library-grade engine.
3. *~regex-automata~ low-level API* — build a custom VM on top of its
   NFA/DFA primitives, reusing its (well-tested, Unicode-aware)
   automaton construction while keeping our own instruction set for
   the parts it can't express.

** Decision Outcome

Chosen option: *Hand-rolled VM* (option 1).

Neither library option can express pass-variable guards or effects —
these are checks against, and mutations of, state external to the
matched text, which has no representation in the theory of regular
languages either crate's API is built on. Both would need a bespoke
layer wrapped around the library to handle them, at which point the
"pure regex" subset the library actually helps with is a minority of
what these rules need — everything involving variables, effects, or
weight-based multi-rule dispatch would still need our own VM.
Building *two* execution models (library automaton for the regular
part, custom interpreter for the rest) and keeping them in sync
seemed like more complexity, not less, for the parts that matter most.

The ~regex~ crate specifically also does not support arbitrary
lookaround or backreferences (a deliberate trade-off for guaranteed
linear-time matching) — so even the "boundary anchor" work
(~EndAnchor~/~NotEndAnchor~, 2026-07-03) would not have come for free
from it; the same external-state-threading (an ~at_start: bool~
computed by the translation loop, since the regexp only ever sees the
remaining suffix and can't determine its own absolute position) would
still have been necessary regardless of the matching backend.

*** Positive Consequences

- Effects, pass-variables, and weight-based rule selection compose
  naturally with the rest of the translation pipeline, since they're
  just more instruction types in the same VM rather than a bolt-on
  layer around a foreign library.
- ~trace~ gets exact per-rule attribution for free — every instruction
  stream carries its own origin rule and precedence.
- Adding a new liblouis construct (as with ~EndAnchor~/~NotEndAnchor~/
  ~Never~ this week) is a small, local, well-understood change: a new
  ~Regexp~ variant, an ~emit~ arm, and one or two VM instruction cases.

*** Negative Consequences

- Slower than a production-grade regex engine (no SIMD, no DFA
  caching) — ~TODO.org~'s still-open "performance" item (offset
  buckets, shrinking the opcode operand) is a standing acknowledgment
  of this cost, not an oversight.
- We own every correctness bug a mature regex engine would have
  already found and fixed. This session alone surfaced several in our
  own engine: ~negate()~ on ~Concat~/~Either~ not honoring De Morgan's
  laws (pre-existing, still open), an infinite VM loop on a
  zero-width-succeeding quantified body (found and fixed 2026-07-03),
  and a missing negation case for the new end-anchor that would have
  panicked (found in review and fixed the same day).

** Pros and Cons of the Options

*** Hand-rolled VM

- Good, because it handles effects, pass-variables, and weight-based
  precedence natively, with no impedance mismatch
- Good, because ~trace~-level introspection is a natural consequence
  of the design, not an add-on
- Bad, because we maintain regex-engine correctness ourselves,
  including edge cases (negation, infinite loops) a mature crate would
  already have solved
- Bad, because raw matching performance is well below what ~regex~/
  ~regex-automata~ achieve

*** ~regex~ crate

- Good, because it is heavily optimized and extensively tested
- Bad, because it has no notion of external state guards or effects —
  every pass-variable-dependent rule would need custom logic wrapped
  around it anyway
- Bad, because it deliberately excludes lookaround/backreferences, so
  boundary-anchor support still needs the same external-state
  workaround we already built
- Bad, because weight-based selection across many independently
  compiled patterns, and per-rule ~trace~ attribution, aren't things
  its API is shaped for

*** ~regex-automata~ low-level API

- Good, because it reuses well-tested, Unicode-aware NFA/DFA
  construction while allowing more custom control than the ~regex~
  crate's high-level API
- Bad, because pass-variables/effects/weight-based dispatch still need
  a separate custom interpreter layered on top, meaning two execution
  models to keep in sync instead of one
- Bad, because this was the option ~TODO.org~ already named and
  rejected for exactly this reason

** Links

- Related to :: [[*Regexps duplicate character transitions][Regexps duplicate character transitions]] (superseded) — prior,
  concrete experience with a shared-automaton approach running into
  the same "independent per-rule precedence" problem from a different
  angle

* COMMENT [short title of solved problem and solution]                     :Template:
# - Status :: [proposed | rejected | accepted | deprecated | ... |
#   superseded by [[file:0005-example.md][ADR-0005]]]
- Deciders :: [list everyone involved in the decision]
- Date :: [YYYY-MM-DD when the decision was last updated]

Technical Story: [description | ticket/issue URL]

** Context and Problem Statement

[Describe the context and problem statement, e.g., in free form using
two to three sentences. You may want to articulate the problem in form
of a question.]

** Decision Drivers

- [driver 1, e.g., a force, facing concern, ...]
- [driver 2, e.g., a force, facing concern, ...]
- ...

** Considered Options

- [option 1]
- [option 2]
- [option 3]
- ...

** Decision Outcome

Chosen option: "[option 1]", because [justification. e.g., only option,
which meets k.o. criterion decision driver | which resolves force force
| ... | comes out best (see below)].

*** Positive Consequences

- [e.g., improvement of quality attribute satisfaction, follow-up
  decisions required, ...]
- ...

*** Negative Consequences

- [e.g., compromising quality attribute, follow-up decisions required,
  ...]
- ...

** Pros and Cons of the Options

*** [option 1]

[example | description | pointer to more information | ...]

- Good, because [argument a]
- Good, because [argument b]
- Bad, because [argument c]
- ...

*** [option 2]

[example | description | pointer to more information | ...]

- Good, because [argument a]
- Good, because [argument b]
- Bad, because [argument c]
- ...

*** [option 3]

[example | description | pointer to more information | ...]

- Good, because [argument a]
- Good, because [argument b]
- Bad, because [argument c]
- ...

** Links

- [Link type] [Link to ADR]
- ...
